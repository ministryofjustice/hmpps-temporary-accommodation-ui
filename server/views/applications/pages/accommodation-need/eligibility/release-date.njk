{% extends "../../layout.njk" %}

{% block questions %}

  {% set pageTitleHTML %}
    <span class="govuk-caption-l">{{ task.title }}</span>
    {{ page.title }}
  {% endset %}

  <h1 class="govuk-heading-l">
    <span class="govuk-caption-l">{{ task.title }}</span>
    {{ page.title }}
  </h1>

  <p class="govuk-body">This date could be their release date from custody, an Approved Premises, or Short-Term Accommodation (CAS2).</p>
  <p class="govuk-body">If they’re being released following Risk Assessed Recall Review (RARR), enter the provisional release date.</p>

  {{ formPageDateInput( {
    fieldName: "releaseDate",
    fieldset: {
      legend: {
        text: "What is their release date?",
        classes: "govuk-fieldset__legend--m"
      }
    },
    hint: {
      text: dateInputHint('future')
    },
    items: dateFieldValues('releaseDate', errors)
  }, fetchContext()) }}

{% endblock %}
