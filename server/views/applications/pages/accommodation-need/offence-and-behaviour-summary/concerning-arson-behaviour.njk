{% extends "../../layout.njk" %}

{% block questions %}
    {% set concerningArsonBehaviourDetailHtml %}
        {{ formPageTextarea({
            fieldName: 'concerningArsonBehaviourDetail',
            label: {
                text: "Explain what the behaviour is and how it could be managed",
                classes: "govuk-label--s"
            }
        }, fetchContext()) }}
    {% endset %}

    <h1 class="govuk-heading-l">
        <span class="govuk-caption-l">{{ task.title }}</span>
        {{ page.title }}
    </h1>

    {{ formPageRadios({
        fieldName: "concerningArsonBehaviour",
        hint: {
            text: "This is about arson behaviour which might introduce risk in an accommodation placement."
        },
        items: [
            {
                value: "yes",
                text: "Yes",
                conditional: {
                html: concerningArsonBehaviourDetailHtml
            }
            },
            {
                value: "no",
                text: "No"
            }
        ]
    }, fetchContext()) }}

{% endblock %}
